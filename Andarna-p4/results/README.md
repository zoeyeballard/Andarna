# results/

Raw profiling data lands here and is **gitignored** (traces and JSON dumps get large).
Commit only small, human-readable summaries (e.g. `baseline/profiling_summary.json`) by
force-adding them: `git add -f results/<path>`.
