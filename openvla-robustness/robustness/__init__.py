"""Observation-pipeline degradation + analysis for OpenVLA robustness study.

Degradations are applied between the simulated camera and the model input — the
model itself is never modified. See ``degradations.py`` for the wrappers and
``analysis.py`` for result aggregation / plotting.
"""
